"use strict";
let admin = ['admin', 'password']

let ajaxEditar = `
    <div>
        <input type="text">
        <button>Aceptar</button>
        <button>Cancelar</button>
    </div>
`;

//formularios
/*
let formLogin = document.querySelector('');
let errorLogin = document.querySelector('');
let formRegister = document.querySelector('');
let successRegister = document.querySelector('');
let errorRegister = document.querySelector('');

//cuando "submit" 'validamos' para form login
formLogin.addEventListener("submit", function (e) {
    e.preventDefault();
    let formData = new FormData(formLogin);
    let email = formData.get("email");
    let password = formData.get("password");
    if (email == 'user' && password == 'user') {
        window.location.href = '../pages/'; //terminar
    } else if (email == 'admin' && password == 'admin'){
        window.location.href = '../pages/'; //terminar
    } else {
        let errorLogin.style.display = 'block'; //se muestra el error q esta hidden en el html
    }
})


//cuando "submit" 'validamos' para form login
formRegister.addEventListener("submit", function (e) {
    e.preventDefault();
    let formData = new FormData(formRegister);
    //hacer lets de todos los inputs
    let  = formData.get("email");
    let  = formData.get("");

    //if de que los inputs no esten vacios
    if ( ==  &&  == '') {
        let successRegister.style.display = 'block'; //se muestra que se mandaron los datos correctamente
    } else {//si los inputs no estan completos
        let errorRegister.style.display = 'block'; //se muestra el error q esta hidden en el html
    }
})
*/